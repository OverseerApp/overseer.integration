# Overseer Integration SDK

SDK for integrating with Overseer, a utility that allows for monitoring multiple 3d printers.

## Installation

Install the NuGet package:

```bash
dotnet add package Overseer.Server.Integration
```